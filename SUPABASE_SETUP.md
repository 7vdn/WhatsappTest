# Supabase Setup Guide

## 📋 خطوات الإعداد

### 1. إنشاء الجدول في Supabase

> ⚠️ **ملاحظة مهمة**: إذا كان لديك جدول `users` قديم يعتمد على `username`، يجب حذفه أولاً أو تعديله لأن النظام الجديد يعتمد على `email`.
> لحذف الجدول القديم (سيحذف كل البيانات):
> ```sql
> DROP TABLE IF EXISTS users;
> ```

1. افتح [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **SQL Editor** من القائمة الجانبية
4. انسخ محتوى ملف `supabase-schema.sql` والصقه في المحرر
5. اضغط **Run** لتنفيذ الأوامر

### 2. إضافة متغيرات البيئة في Coolify

اذهب إلى **Environment Variables** في Coolify وأضف:

```
SUPABASE_URL=https://dgajxdvyxhuufwdshihx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRnYWp4ZHZ5eGh1dWZ3ZHNoaWh4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjU4NDczNjksImV4cCI6MjA4MTQyMzM2OX0.C3z8hickkgtQsJXkzwTdtcLs49a5UVsH7YG5OAnqXP4
```

### 3. إعادة النشر

اضغط **Redeploy** في Coolify

## ✅ التحقق من النجاح

بعد إعادة النشر، في **Logs** يجب أن ترى:
```
Using Supabase storage
```

## 🎯 المميزات

- ✅ البيانات تبقى محفوظة حتى لو السيرفر انطفى
- ✅ تقدر تسجل دخول بنفس الحساب في أي وقت
- ✅ كل المستخدمين محفوظين في قاعدة بيانات دائمة
- ✅ عداد الرسائل يتحدث تلقائياً

## 🔍 استكشاف الأخطاء

### المشكلة: لا يزال يستخدم in-memory storage

**الحل**: تأكد من أن متغيرات البيئة مضبوطة بشكل صحيح في Coolify

### المشكلة: خطأ في إنشاء المستخدم

**الحل**: تأكد من تنفيذ ملف `supabase-schema.sql` في Supabase SQL Editor

## 📊 عرض البيانات

لعرض المستخدمين المسجلين:
1. اذهب إلى **Table Editor** في Supabase
2. اختر جدول `users`
3. شوف كل المستخدمين المسجلين
