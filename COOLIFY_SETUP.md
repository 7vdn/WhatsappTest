# خطوات إصلاح مشكلة النشر على Coolify

## 🔴 المشكلة
التطبيق يعمل (Running) لكن عند فتح الرابط يظهر خطأ 404

## ✅ الحل

### 1. إعدادات متغيرات البيئة (Environment Variables)
في Coolify، اذهب إلى **Environment Variables** وأضف:

```
PORT=3000
NODE_ENV=production
```

### 2. إعدادات الشبكة (Network)
تأكد من أن:
- **Ports Exposes**: `3000`
- **Ports Mappings**: فارغ (اتركه كما هو)

### 3. إعدادات البناء (Build)
في **Configuration** → **General**:
- **Build Pack**: `Nixpacks` ✅
- **Is it a static site?**: غير مفعل ❌

### 4. إعدادات Healthcheck ⭐ (مهم جداً)
في **Configuration** → **Healthcheck**:

| الإعداد | القيمة |
|---------|--------|
| **Path** | `/health` |
| **Port** | `3000` |
| **Interval** | `30s` |
| **Timeout** | `5s` |
| **Retries** | `3` |
| **Start Period** | `60s` |

> **ملاحظة مهمة**: بدون ضبط الـ healthcheck، راح يظهر التطبيق كـ "Degraded (unhealthy)"

### 5. إعادة النشر (Redeploy)
بعد تحديث الإعدادات:
1. اذهب إلى **Deployments**
2. اضغط على **Redeploy** أو **Restart**
3. انتظر حتى ينتهي البناء

### 6. التحقق من السجلات (Logs)
في **Logs**، يجب أن ترى:
```
building client...
building server...
serving on port 3000
```

## 🔍 استكشاف الأخطاء

### إذا استمرت المشكلة:
1. تحقق من **Terminal** في Coolify وشغل:
   ```bash
   ls -la dist/
   ls -la dist/public/
   ```
   يجب أن ترى ملفات HTML/JS/CSS

2. تحقق من أن البناء نجح:
   ```bash
   cat dist/index.cjs
   ```
   يجب أن يكون ملف موجود

3. تحقق من أن السيرفر يعمل:
   ```bash
   ps aux | grep node
   ```

### إذا كان مجلد dist فارغ:
المشكلة في عملية البناء. تحقق من:
- أن `npm run build` يعمل بدون أخطاء
- أن جميع dependencies مثبتة

### إذا ظهر "Degraded (unhealthy)":
راجع ملف [HEALTHCHECK.md](HEALTHCHECK.md) للحصول على تعليمات مفصلة حول ضبط الـ healthcheck.

## 📋 ملاحظات مهمة
- ملف `nixpacks.toml` يحتوي على جميع إعدادات البناء التلقائية
- لا تحتاج لتعديل أوامر Install/Build/Start يدوياً
- تأكد من أن الـ Port في متغيرات البيئة يطابق Port Exposes
- **الـ healthcheck ضروري** لكي يظهر التطبيق كـ "Healthy"

## 🎯 الخطوة التالية
بعد تطبيق هذه الإعدادات، قم بإعادة النشر وانتظر 2-3 دقائق حتى يكتمل البناء.
