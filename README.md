# WhatsApp API - Baileys QR Bot

## 📋 المتطلبات
- Node.js 22 أو أحدث
- npm أو yarn

## 🚀 التشغيل المحلي

### 1. تثبيت الحزم
```bash
npm install
```

### 2. تشغيل في وضع التطوير
```bash
npm run dev
```

### 3. بناء المشروع للإنتاج
```bash
npm run build
```

### 4. تشغيل في وضع الإنتاج
```bash
npm run start
```

## 🌐 النشر على Coolify

### الخطوات:
1. **ربط المستودع**: قم بربط مستودع GitHub مع Coolify
2. **إعدادات البناء**:
   - Build Pack: `Nixpacks`
   - Port: `3000` (أو حسب متغير البيئة `PORT`)
   
3. **متغيرات البيئة المطلوبة**:
   ```
   PORT=3000
   NODE_ENV=production
   ```

4. **الأوامر** (يتم تكوينها تلقائياً من `nixpacks.toml`):
   - Install: `npm ci`
   - Build: `npm run build`
   - Start: `npm run start`

### ملاحظات مهمة:
- تأكد من أن Port في Coolify يطابق متغير البيئة `PORT`
- المشروع يحتاج إلى بناء (build) قبل التشغيل في الإنتاج
- ملف `nixpacks.toml` يحتوي على جميع إعدادات البناء

## 🔧 استكشاف الأخطاء

### المشكلة: الصفحة لا تظهر (404)
**الحل**: 
1. تأكد من أن المشروع تم بناؤه بنجاح
2. تحقق من أن مجلد `dist/public` موجود بعد البناء
3. تأكد من أن متغير البيئة `NODE_ENV=production`
4. راجع سجلات (logs) التطبيق في Coolify

### المشكلة: Port غير صحيح
**الحل**: 
- تأكد من أن Port في Coolify يطابق متغير البيئة `PORT`
- البورت الافتراضي هو `5000` لكن يمكن تغييره عبر متغير البيئة

## 📁 هيكل المشروع
```
.
├── client/          # Frontend (React + Vite)
├── server/          # Backend (Express)
├── shared/          # Shared types
├── script/          # Build scripts
├── dist/            # Built files (generated)
│   ├── public/      # Frontend build
│   └── index.cjs    # Server build
├── nixpacks.toml    # Coolify/Nixpacks config
└── package.json
```

## 🔐 الأمان
- لا تنسى إضافة متغيرات البيئة الحساسة في Coolify
- تأكد من تحديث `.gitignore` لعدم رفع الملفات الحساسة

## 📝 الترخيص
MIT
